package com.example.insurancegateway;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class InsuranceGatewayApplication {

	public static void main(String[] args) {
		SpringApplication.run(InsuranceGatewayApplication.class, args);
	}
    /*
    Task 1 - Create a home page to select the type of insurance that can capture user details
    to display the different plans available within that insurance

     GET CUSTOMER INFO, such as address first -> person info with bod -> income

     */

	/*
	Register page

	Users can create an account using email and password
	user information is changeable upon home page after login such as address and firstname and lastname

	 */
}
